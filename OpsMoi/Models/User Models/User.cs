using Airconditioning.Interfaces;
using System.Collections.Generic;
using System.Linq;

namespace Airconditioning.Models
{
    /*public class User : Employee
    {
        
       
        //public employeeType employeeType { get { return Models.employeeType.مستخدم; } }

        
        public static User GetUser(int id, string userName, string passWord, string name, string address, string phonenumber, string nationalID, string email, int branch, int permissionLevel, string Permissions)
              => new User() { id = id, user_name = userName, password = passWord, name = name, address = address, phonenumber = phonenumber, national_id = nationalID, email = email, branch = branch, permission_level = permissionLevel, permissions = Permissions };
        
    }*/
}